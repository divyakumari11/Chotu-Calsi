package practise2;

public class growSize {
	int temp[] = null; 
    if (count == size) { 

        double size;
		// temp is a double size array of array 
        // and store array elements 
        temp = new int[(int) (size * 2)]; 
        { 
            for (int i = 0; i < size; i++) { 
                int[] array = null;
				// copy all array value into temp 
                temp[i] = array[i]; 
            } 
        } 
    } 

}
}
